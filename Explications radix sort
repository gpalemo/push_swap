Après index_stack, on remplace chaque valeur par son index trié (0..4).

Trié: 1 2 3 4 5
Donc indexes :

1 → 0

2 → 1

3 → 2

4 → 3

5 → 4

Donc ta stack A devient (dans l’ordre d’entrée 5 3 1 4 2) :
A = [4, 2, 0, 3, 1]
B est vide.

Tu fais plusieurs “passes” (i = bit 0, bit 1, bit 2...).
À chaque passe, tu regardes le bit i du nombre au sommet de A :

si bit = 0 → pb (tu l’envoies dans B)

si bit = 1 → ra (tu le gardes dans A mais tu le mets en bas)

Tu répètes ça exactement size fois (donc tu traites chaque élément une fois).
Puis tu remontes tout de B vers A avec pa.


PASS 1 : i = 0 (bit 0, le bit “pair/impair”)

A au début : [4, 2, 0, 3, 1]
Bit0 de chaque :

4 = 100 → bit0 = 0

2 = 010 → bit0 = 0

0 = 000 → bit0 = 0

3 = 011 → bit0 = 1

1 = 001 → bit0 = 1

Ton code fait j de 0 à 4 (5 tours) :

top = 4 → bit0=0 → pb
A: [2,0,3,1] B: [4]

top = 2 → bit0=0 → pb
A: [0,3,1] B: [2,4]

top = 0 → bit0=0 → pb
A: [3,1] B: [0,2,4]

top = 3 → bit0=1 → ra
A: [1,3] B: [0,2,4]

top = 1 → bit0=1 → ra
A: [3,1] B: [0,2,4]

Ensuite tu fais :


while (*stack_b)
    pa(stack_a, stack_b);

Donc tu remontes B vers A (3 fois) :

A devient : [0,2,4,3,1] (l’idée : tous les bit0=0 reviennent au-dessus)

👉 Résultat après bit0 :
Les nombres sont regroupés “bit0=0” puis “bit0=1”.

PASS 2 : i = 1 (bit 1)

A : [0,2,4,3,1]
Bit1 :

0 = 000 → 0

2 = 010 → 1

4 = 100 → 0

3 = 011 → 1

1 = 001 → 0

On refait 5 tours :

0 bit1=0 → pb

2 bit1=1 → ra

4 bit1=0 → pb

3 bit1=1 → ra

1 bit1=0 → pb

Puis pa jusqu’à vider B.

👉 Résultat après bit1 :
On a maintenant des groupes plus fins (par “paires” de bits), et surtout :
on garde l’ordre relatif (Si 2 était avant 5 au début, il reste avant 5 après l’opération) grâce à ra (ça rend le tri stable à chaque passe).

PASS 3 : i = 2 (bit 2)

Même principe, et à la fin de cette passe, comme on a traité tous les bits nécessaires (0..2), l’ordre est complètement trié :

A = [0,1,2,3,4]
Puis si tu veux revenir aux vraies valeurs, ça correspond à 1 2 3 4 5 (mais toi tu gardes les indexes, c’est ok)


“Je transforme les valeurs en indexes 0..n-1, puis je trie en binaire, bit par bit.
À chaque bit, je parcours exactement n éléments : si le bit vaut 0 je push dans B, sinon je rotate A. Puis je remonte tout B dans A. Après tous les bits, A est triée.”

I shift the number to the right by i to bring the target bit to the last position, then I isolate it with & 1.
If the result is 0 I push to B, otherwise I rotate A.

“For each bit, numbers with a 0 are pushed to stack B and numbers with a 1 are rotated in stack A.
This keeps the relative order and progressively sorts the list from the least significant bit to the most significant bit.


